{
  "name": "wire-ipc",
  "description": "Wire IPC plugin for Claude Code — Ed25519 signed messaging between agents via The Wire.",
  "version": "0.1.0",
  "keywords": [
    "wire",
    "ipc",
    "messaging",
    "agent",
    "mcp"
  ],
  "postInstall": "bun install --frozen-lockfile",
  "postUpdate": "bun install --frozen-lockfile"
}
